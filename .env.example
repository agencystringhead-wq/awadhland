# Copied into the Cloudflare Pages build environment (and into .env.local for a local run).
# Base URL of the awadhland-leads Worker (worker/). Unset → forms compose a WhatsApp message and
# analytics is a no-op, which is what preview builds should do.
NEXT_PUBLIC_LEAD_ENDPOINT=https://lead.awadhland.com
