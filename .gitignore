node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.env*
!.env.example
.DS_Store
.claude/
.playwright-mcp/

# dev-only placeholder media, never shipped (scripts/check-dev-assets.ts fails a CI build that references it)
/public/dev/

# Open Graph images, rendered at build by scripts/generate-og.ts
/public/og/

# Scanned IGRSUP rate-list PDFs (84.5 MB). Provenance only; the archived copies live on R2.
# The transcribed CSVs beside them ARE committed.
/data/sources/**/*.pdf

# Per-tehsil rate search chunks, generated at build by scripts/build-rate-chunks.ts
/public/rates/
